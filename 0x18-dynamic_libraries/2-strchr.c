#include <string.h>
#include "main.h"

/**
* _strchr - functions that locates the character in a string
* @s: pointer to the string s searched for
* @c: character to be found
*
* Return: pointer to the first chracter s
*/

char *_strchr(char *s, char c)
{

return (strchr(s, c));

}
