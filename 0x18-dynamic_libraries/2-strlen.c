#include <stdio.h>

/**
* _strlen - fuction that returns the lenght of a string
* @s: s is a character
* Return: value is i
*/
int _strlen(char *s)
{
int i;

for (i = 0; s[i] != '\0'; i++)
{

}
return (i);
}
